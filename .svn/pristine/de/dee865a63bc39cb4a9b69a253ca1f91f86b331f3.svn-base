describe('Dashboard.view.alerts.currentAlert.MainController', function() {
	var controller = null;
	controller = new Dashboard.view.alerts.currentAlert.MainController();
	describe('getFilterValues()', function() {
		it('should be []', function() {
			controller = new Dashboard.view.alerts.currentAlert.MainController();
			var filter = controller.getFilterValues();
			expect(filter).toEqual([]);
		});
	});

	describe('getFilters()', function() {
		it('controller.feature.data.configuration should not have configuration', function() {
			var filter = controller.getFilters();
			expect(filter).toEqual([]);
		});
	});
});